import SideBarDesktop from "@/components/layout/SideBarDesktop";
import AddPlanningForm from "@/components/planning/add-planning/AddPlanningForm";

const page = () => {
  return (
    <SideBarDesktop>
      <AddPlanningForm />
    </SideBarDesktop>
  );
};

export default page;
